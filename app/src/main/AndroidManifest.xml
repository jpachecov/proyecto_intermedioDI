<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jeanpierrepachecoavila.proyecto_intermedio" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".Actividad_prinicipal"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".llena_formulario"
            android:label="@string/title_activity_llena_formulario" >
        </activity>
        <activity
            android:name=".segundaParte"
            android:label="@string/title_activity_segunda_parte" >
        </activity>
    </application>

</manifest>
